# Example for tauri-plugin-system-info

1. Run `npm i` to install dependencies.
2. Run `npm run tauri dev` to start dev app.
3. Right click to open console. Check data logged in console
   1. There is too much data, I simply logged the data out for now.
   2. I may add some UI components in the future.
